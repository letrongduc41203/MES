<UserControl x:Class="MES.Views.Orders"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="800" 
        Width="1400"
        Background="#f8f9fa">

    <UserControl.Resources>
        <!-- Header Style -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Box Style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                            Margin="{TemplateBinding Padding}"/>
                                <TextBlock Text="Search orders..." 
                                          Foreground="#999" 
                                          Margin="{TemplateBinding Padding}"
                                          IsHitTestVisible="False">
                                    <TextBlock.Visibility>
                                        <Binding Path="Text" 
                                                 RelativeSource="{RelativeSource TemplatedParent}">
                                            <Binding.Converter>
                                                <BooleanToVisibilityConverter/>
                                            </Binding.Converter>
                                        </Binding>
                                    </TextBlock.Visibility>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007acc"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#f0f0f0"/>
            <Setter Property="AlternatingRowBackground" Value="#fafafa"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#34495e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#2c3e50"/>
        </Style>

        <!-- Status Badge Style -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- Add BooleanToVisibilityConverter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Orders Management" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Manage production orders and track their progress" 
                          FontSize="14" 
                          Foreground="#7f8c8d"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">

                <Button Content="🔄 Refresh" 
                       Style="{StaticResource ActionButtonStyle}"
                       Background="#3498db" 
                       Foreground="White"
                       Margin="0,0,10,0"
                       Click="Refresh_Click"/>

                <Button Content=" New Order" 
                       Style="{StaticResource ActionButtonStyle}"
                       Background="#27ae60" 
                       Foreground="White"
                       Click="NewOrder_Click"/>

                <Button Content=" Assign Employees"
                        Style="{StaticResource ActionButtonStyle}"
                        Background="#8e44ad"
                        Foreground="White"
                        Margin="10,0,0,0"
                        Click="AssignEmployees_Click"/>
            </StackPanel>

        </Grid>



        <!-- Orders DataGrid -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                BorderBrush="#e0e0e0" 
                BorderThickness="1">
            <DataGrid x:Name="OrdersDataGrid" 
                     Style="{StaticResource ModernDataGridStyle}"
                     IsReadOnly="True">

            <DataGrid.Resources>
                <!-- Style for all DataGrid cells -->
                <Style TargetType="TextBlock" x:Key="CenteredCellStyle">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="5,3"/>
                </Style>
                
                <!-- Apply centered style to all DataGrid cells -->
                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Grid Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" 
                                                    HorizontalAlignment="Center"
                                                    TextBlock.TextAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                
                <!-- Center align DataGrid column headers -->
                <Style x:Key="CenteredHeaderStyle" TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </DataGrid.Resources>
                <DataGrid.Columns>
                    <!-- Order ID -->
                    <DataGridTextColumn Header="Order ID" 
                                       Binding="{Binding OrderId}" 
                                       Width="100"
                                       HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- Product ID -->
                    <DataGridTextColumn Header="Product ID" 
                                       Binding="{Binding ProductId}" 
                                       Width="100"
                                       HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- Product -->
                    <DataGridTextColumn Header="Product" 
                                       Binding="{Binding ProductName}" 
                                       Width="250"
                                       HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- Quantity -->
                    <DataGridTextColumn Header="Quantity" 
                                       Binding="{Binding Quantity}" 
                                       Width="100"
                                       HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <DataGridTextColumn Header="Machine" 
                                       Binding="{Binding MachineName}" 
                                       Width="150"
                                       HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- Assigned Employees -->
                    <DataGridTextColumn Header="Assign" 
                                       Binding="{Binding AssignedEmployees}" 
                                       Width="200"
                                       HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- Start Time -->
                    <DataGridTextColumn Header="Start Time"
                                        Binding="{Binding StartTime, StringFormat='dd/MM/yyyy HH:mm'}"
                                        Width="150"
                                        HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- End Time -->
                    <DataGridTextColumn Header="End Time"
                                        Binding="{Binding EndTime, StringFormat='dd/MM/yyyy HH:mm'}"
                                        Width="150"
                                        HeaderStyle="{StaticResource CenteredHeaderStyle}"/>

                    <!-- Status -->
                    <DataGridTemplateColumn Header="Status" Width="170" HeaderStyle="{StaticResource CenteredHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" 
                                       Padding="8,4" 
                                       HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                    <Setter Property="Background" Value="#f39c12"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Processing">
                                                    <Setter Property="Background" Value="#3498db"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                    <Setter Property="Background" Value="#27ae60"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="On Hold">
                                                    <Setter Property="Background" Value="#95a5a6"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                    <Setter Property="Background" Value="#e74c3c"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" 
                                              Foreground="White" 
                                              FontSize="11" 
                                              FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>


                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer Section -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Statistics -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Border Background="#3498db" CornerRadius="20" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding TotalOrders, StringFormat='Total: {0} Orders'}" 
                                  Foreground="White" 
                                  FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Background="#27ae60" CornerRadius="20" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✅" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding CompletedOrders, StringFormat='Completed: {0}'}" 
                                  Foreground="White" 
                                  FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Background="#f39c12" CornerRadius="20" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏳" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding PendingOrders, StringFormat='Pending: {0}'}" 
                                  Foreground="White" 
                                  FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Background="#3498db" CornerRadius="20" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ProcessingOrders, StringFormat='Processing: {0}'}" 
                                  Foreground="White" 
                                  FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <!-- <Border Background="#e74c3c" CornerRadius="20" Padding="15,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Overdue: 12" 
                                  Foreground="White" 
                                  FontWeight="SemiBold"/>
                    </StackPanel>
                </Border> -->
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>