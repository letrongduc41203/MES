<UserControl x:Class="MES.Views.Materials"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Color Scheme -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2C3E50"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#34495E"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3498DB"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#27AE60"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F39C12"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E74C3C"/>
        <SolidColorBrush x:Key="LightBrush" Color="#ECF0F1"/>
        <SolidColorBrush x:Key="InfoBrush" Color="#17A2B8"/>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.15" BlurRadius="10"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="{TemplateBinding Tag}" 
                                          FontFamily="Segoe MDL2 Assets" 
                                          FontSize="14" 
                                          Margin="0,0,8,0" 
                                          VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Material Card Style -->
        <Style x:Key="MaterialCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="MinHeight" Value="180"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="3" Opacity="0.2" BlurRadius="12"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Progress Bar Style -->
        <Style x:Key="MaterialProgressStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="12"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <Border Name="PART_Track" CornerRadius="6">
                                <Border Name="PART_Indicator" 
                                       Background="{TemplateBinding Foreground}" 
                                       HorizontalAlignment="Left" 
                                       CornerRadius="6"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource LightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Action Bar -->
        <Border Grid.Row="0" Background="White" Padding="30,20" Margin="0,0,0,20" 
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="btnAddMaterial" 
                           Background="{StaticResource SuccessBrush}" 
                           Style="{StaticResource ActionButtonStyle}"
                           Tag="&#xE710;"
                           Click="AddMaterial_Click">
                        Add Material
                    </Button>
                </StackPanel>

                <!-- Search and Filter -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBox x:Name="txtSearch" 
                            Width="200" 
                            Height="35" 
                            Padding="10,0,35,0" 
                            VerticalContentAlignment="Center"
                            Text="Search materials..."
                            Foreground="Gray"
                            BorderBrush="#E0E0E0"
                            GotFocus="SearchBox_GotFocus"
                            LostFocus="SearchBox_LostFocus"
                            TextChanged="SearchBox_TextChanged"/>
                    <Button Background="Transparent" 
                           BorderThickness="0" 
                           Margin="-30,0,10,0"
                           Cursor="Hand">
                        <TextBlock Text="&#xE721;" 
                                  FontFamily="Segoe MDL2 Assets" 
                                  Foreground="#7F8C8D"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary Cards -->

        <!-- Category Tabs -->
        <Border Grid.Row="2" Background="White" Margin="20,0,20,20" Padding="0" 
                BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8"/>

        <!-- Material Details Grid -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="20,0,20,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                          Text="Material Inventory Details" 
                          FontSize="18" 
                          FontWeight="Bold" 
                          Margin="0,0,0,15"/>

                <DataGrid x:Name="materialDataGrid" 
                         Grid.Row="1"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         Background="Transparent"
                         BorderThickness="0"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column"
                         AlternatingRowBackground="#F8F9FA"
                         SelectionChanged="MaterialDataGrid_SelectionChanged">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding MaterialId}" Width="80"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding MaterialName}" Width="300"/>
                        <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="100"/>
                        <DataGridTextColumn Header="StockQuantity" Binding="{Binding StockQuantity}" Width="100"/>
                        <DataGridTextColumn Header="Last Updated" Binding="{Binding LastUpdated, StringFormat=d}" Width="350"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>