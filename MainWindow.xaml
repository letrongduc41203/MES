<Window x:Class="MES.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PCB Production Management System" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Window.Resources>
        <!-- Color Scheme -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2C3E50"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#34495E"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3498DB"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#27AE60"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F39C12"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E74C3C"/>
        <SolidColorBrush x:Key="LightBrush" Color="#ECF0F1"/>
        <SolidColorBrush x:Key="DarkBrush" Color="#2C3E50"/>
        
        <!-- Menu Button Style -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
         
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{TemplateBinding Tag}" 
                                          FontFamily="Segoe MDL2 Assets" 
                                          FontSize="16" 
                                          Margin="0,0,15,0" 
                                          VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Active Menu Button Style -->
        <Style x:Key="ActiveMenuButtonStyle" TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource DarkBrush}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Sidebar Menu -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Logo/Title -->
                <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="PCB Production" 
                                  FontSize="18" 
                                  FontWeight="Bold" 
                                  Foreground="White" 
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="Management System" 
                                  FontSize="12" 
                                  Foreground="#BDC3C7" 
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- Menu Items -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,10,0,0">
                        <Button x:Name="btnDashboard" 
                               Style="{StaticResource ActiveMenuButtonStyle}" 
                               Tag="&#xE80F;"
                               Click="MenuButton_Click">
                            Dashboard
                        </Button>
                        <Button x:Name="btnOrders" 
                               Style="{StaticResource MenuButtonStyle}" 
                               Tag="&#xE8A1;"
                               Click="MenuButton_Click" Content="Orders (Lệnh sản xuất)"/>

                        
                        
                        <Button x:Name="btnMaterials" 
                               Style="{StaticResource MenuButtonStyle}" 
                               Tag="&#xE8EF;"
                               Click="MenuButton_Click">
                            Materials (Nguyên liệu)
                        </Button>
                        
                        <Button x:Name="btnMachines" 
                               Style="{StaticResource MenuButtonStyle}" 
                               Tag="&#xE950;"
                               Click="MenuButton_Click">
                            Machines (Máy móc)
                        </Button>
                        
                        <Button x:Name="btnEmployees" 
                               Style="{StaticResource MenuButtonStyle}" 
                               Tag="&#xE716;"
                               Click="MenuButton_Click">
                            Employees (Nhân viên)
                        </Button>
                        
                        <Button x:Name="btnReports" 
                               Style="{StaticResource MenuButtonStyle}" 
                               Tag="&#xE9F9;"
                               Click="MenuButton_Click">
                            Reports (Báo cáo)
                        </Button>
                        
                        
                    </StackPanel>
                </ScrollViewer>
                
                <!-- User Info -->
                <Border Grid.Row="2" Background="{StaticResource SecondaryBrush}" Padding="20,15">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="40" Height="40" Fill="{StaticResource AccentBrush}"/>
                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Admin User" 
                                      FontWeight="Bold" 
                                      Foreground="White" 
                                      FontSize="12"/>
                            <TextBlock Text="Administrator" 
                                      Foreground="#BDC3C7" 
                                      FontSize="10"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        
        <!-- Content Area -->
        <Grid Grid.Column="1" Background="{StaticResource LightBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header Bar -->
            <Border Grid.Row="0" Background="White" Padding="30,20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="txtPageTitle" 
                                  Text="Dashboard" 
                                  Style="{StaticResource HeaderStyle}"/>
                        <TextBlock x:Name="txtPageDescription" 
                                  Text="Overview of PCB production activities" 
                                  Foreground="#7F8C8D" 
                                  FontSize="14"/>
                    </StackPanel>
                    
                    
                </Grid>
            </Border>
            
            <!-- Dynamic Content Area -->
            <ContentControl x:Name="MainContentControl" Grid.Row="1" Margin="20">
                
            </ContentControl>
        </Grid>
    </Grid>
</Window>